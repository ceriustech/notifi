import { createMetaData } from '~/site_header_data/utils';

export const meta = createMetaData('AUTH');

const Auth = () => {
	return (
		<main>
			<div>Auth</div>
		</main>
	);
};

export default Auth;
