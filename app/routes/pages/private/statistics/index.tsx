import { createMetaData } from '~/site_header_data/utils';

export const meta = createMetaData('STATISTICS');

const Statistics = () => {
	return <div>Statistics</div>;
};

export default Statistics;
